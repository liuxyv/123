#include <stdio.h>
#include <stdlib.h>

int main()
{
   double n,sum,ave,salay;
   scanf("%f\n",&salay);
   for(n=1.0;n++;){

    if(salay=-1)break;
    else sum=sum+salay;
   }
   ave=sum/(n-1);
   printf("%f\n",ave);
    return 0;
}
